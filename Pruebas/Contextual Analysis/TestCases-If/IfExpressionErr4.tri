! If Expression error test

let
  var c : Boolean;
  var b : Char;
  const a ~ 2;
  var p : Boolean
in
    begin
        c := (ord(b) = a);
        let
            const c ~ 2
        in
            p := 
                if (c) then true ! in this scope, c is integer
                else false
    end