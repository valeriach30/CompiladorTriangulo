! Test of expressions in if command
! It does not run correctly

let
  var a : Integer;
  var b : Char
in
  if a then  ! expression is not boolean
    begin
      putint(a);
    end
  else 
    begin
      put(b);
    end