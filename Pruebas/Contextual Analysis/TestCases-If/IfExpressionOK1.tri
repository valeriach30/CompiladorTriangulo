! Test of If Expression as an expression of an If Command
! It runs correctly

let
  var a : Integer;
  var b : Integer
in
  if (if a < 5 then true else false) ! If boolean expresion
  then
    putint(a)
  else
    putint(b)