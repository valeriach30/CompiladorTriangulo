! Test of If Expression as an expression of an If Command
! It does not run correctly

let
  var a : Integer;
  var b : Integer
in
  if (if c < 5 then true else false) ! c is not declared
  then
    putint(a)
  else
    putint(b)