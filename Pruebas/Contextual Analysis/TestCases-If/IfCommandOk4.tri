! If Command successful test

let
    var a : Integer;
    var b : Integer
in 
    let 
        var a : Boolean
    in
        if (a) ! in this scope, a is boolean
        then
            put('a')
        else
            put('b')