! Test of begin
! It doesn't run correctly

begin
    let
        var i : Integer;
        var y : Integer
    in 
        begin
            let
                var y : Char
            in 
                y := 2 !types mismatch, here 'y' is of type Char
        end;
        y := 8;
        putint(y)
end